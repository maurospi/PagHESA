/* Integración conservada por compatibilidad: la funcionalidad Web Speech API vive en accesibilidad/js/accessibility.js para mantener un panel único. */
(function(){'use strict';window.HESASpeechReady=true;})();
